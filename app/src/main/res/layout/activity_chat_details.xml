<?xml version="1.0" encoding="utf-8"?>
<layout
		xmlns:android="http://schemas.android.com/apk/res/android"
		xmlns:app="http://schemas.android.com/apk/res-auto"
		xmlns:tools="http://schemas.android.com/tools"
		>

	<data>

		<variable
				name="vm"
				type="com.tsic.ui.screen.chatdetails.ChatDetailsViewModel"
				/>

		<variable
				name="activity"
				type="com.tsic.ui.screen.chatdetails.ChatDetailsActivity"
				/>
	</data>

	<androidx.coordinatorlayout.widget.CoordinatorLayout
			android:id="@+id/root_layout"
			android:layout_width="match_parent"
			android:layout_height="match_parent"
			android:background="@drawable/bg_all_white"
			android:fitsSystemWindows="true"
			tools:context=".ui.ChatDetailsActivity"
			>

		<include
				android:id="@+id/content_chat_message"
				layout="@layout/content_chat_details"
				app:activity="@{activity}"
				app:viewModel="@{vm}"
				/>
	</androidx.coordinatorlayout.widget.CoordinatorLayout>
</layout>
